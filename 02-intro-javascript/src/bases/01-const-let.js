// variables y constantes

const nombre = 'Isaac';
const apellido = 'Muñoz';

let valorDado = 5;
valorDado = 4;

console.log(nombre, apellido, valorDado)

if ( true ) {
    const nombre = 'Test';
    console.log(nombre);
}

/*
Notas:
- Las variables var no se tienen que usar.
- Las variables con let y con const, tienen scope asignado. No dan problemas cuando se combinan diferentes scopes.

*/